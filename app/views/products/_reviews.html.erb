<% @product.reviews.each do |review| %>
  <p>
  <%= review.comment %>
  <% if review.user != nil %> <br />
    <em> by <%= review.user.name %></em>
    Added on: <%= review.created_at %>
  <% end %>
  </p>
<% end %>